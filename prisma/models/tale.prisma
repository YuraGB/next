model Tale {
  id               String   @id @default(auto()) @map("_id") @db.ObjectId
  title            String
  mainImage        String
  images           String[]
  createdAt        DateTime @default(now())
  forAge           String
  shortDescription String
  content          String
}
